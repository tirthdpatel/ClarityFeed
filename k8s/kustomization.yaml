# kubectl apply -k k8s/
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: clarity

resources:
  - 00-namespace.yaml
  - 01-configmap.yaml
  - 02-secret.yaml
  - 10-postgres.yaml
  - 20-api.yaml
  - 21-collector-cronjob.yaml
  - 22-worker.yaml
  - 30-ingress.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: clarityfeed
